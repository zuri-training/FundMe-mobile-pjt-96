import 'package:flutter/material.dart';
import 'package:fund_me_zuri/Screens/Bensignup/components/body.dart';
class Bensignup extends StatelessWidget {
  const Bensignup({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Body(),);
  }
}
